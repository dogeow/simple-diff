Native macOS icon using the existing generated artwork with an opaque color background.
Regenerate Assets.car with: bash scripts/build-macos-icon.sh
This avoids the system fallback white platter while preserving ICNS for older macOS.
